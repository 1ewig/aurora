/**
 * Aurora — src/app/(auth)/verify/loading.tsx
 *
 * Loading skeleton for the verify page.
 */

import { AuthSkeleton } from "@/components/auth/AuthSkeleton";

export default function VerifyLoading() {
  return <AuthSkeleton />;
}
