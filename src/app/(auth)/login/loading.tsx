/**
 * Aurora — src/app/(auth)/login/loading.tsx
 *
 * Loading skeleton for the login page.
 */

import { AuthSkeleton } from "@/components/auth/AuthSkeleton";

export default function LoginLoading() {
  return <AuthSkeleton />;
}
