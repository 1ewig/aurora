/**
 * Aurora — src/app/(auth)/reset-password/loading.tsx
 *
 * Loading skeleton for the reset-password page.
 */

import { AuthSkeleton } from "@/components/auth/AuthSkeleton";

export default function ResetPasswordLoading() {
  return <AuthSkeleton />;
}
