/**
 * Aurora — src/app/(auth)/register/loading.tsx
 *
 * Loading skeleton for the register page.
 */

import { AuthSkeleton } from "@/components/auth/AuthSkeleton";

export default function RegisterLoading() {
  return <AuthSkeleton />;
}
